name: Build
run-name: "Build #${{ github.run_number }}"

on:
  push:
    tags:
      - "*.*.*"
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4.1.1

      - name: Get the version
        id: get_version
        run: echo ::set-output name=version::${GITHUB_REF#refs/tags/}

      - name: Set the version
        id: set_version
        run: |
          DEV=${{ steps.get_version.outputs.version }}
          sed -i -e "s/0.0.0-indev/${DEV}/g" gradle.properties

      - name: Set up JDK 17
        uses: actions/setup-java@v4.0.0
        with:
          java-version: '17'
          distribution: 'adopt'
          cache: 'gradle'

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
        with:
          cache-write-only: false
          generate-job-summary: false
          gradle-home-cache-includes: |
            caches
            notifications
            jdks
            wrapper

      - name: Validate Gradle wrapper
        uses: gradle/wrapper-validation-action@v1.1.0

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build
        uses: gradle/gradle-build-action@v2.11.1
        with:
          arguments: build

      # - name: Release
      #   uses: Kir-Antipov/mc-publish@v3.3
      #   with:
      #     modrinth-id: lNttW2Xl
      #     modrinth-token: ${{ secrets.MODRINTH_API_TOKEN }}
      #     modrinth-dependencies: |
      #       JaCEZUhg(required)
      #       7tG215v7(required)

      #     curseforge-id: 513402
      #     curseforge-token: ${{ secrets.CF_API_TOKEN }}
      #     curseforge-dependencies: |
      #       gregtechceu-modern(required)
      #       terrafirmacraft(required)

      #     files: |
      #       build/libs/!(*-@(dev|sources|javadoc)).jar
      #       build/libs/*-@(dev|sources|javadoc).jar

      #     name: TerraFirmaGreg-Core-1.20.x-${{ steps.get_version.outputs.version }}
      #     version: ${{ steps.get_version.outputs.version }}
      #     version-type: release

      #     loaders: forge
      #     game-versions: 1.20.1
      #     game-version-filter: none
      #     java: 17
      #     retry-attempts: 2
      #     retry-delay: 10000
      #     fail-mode: fail

      - name: Commit mod
        run: |
          cd build/libs/
          
          git add .
          git config --global credential.helper '!f() { 
              echo "username=Xikaro"; 
              echo "password=${{ secrets.USER_TOKEN_XIKARO }}"; }; f'
          git commit -m "Automatically update core" -a
          git rm TerraFirmaGreg-Core-1.20.x-*.jar
          git commit -m "Remove core old files"

      - name: Push mod
        uses: ad-m/github-push-action@v0.8.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
          repository: https://github.com/TerraFirmaGreg-Team/Mods-1.20.x
